#include <iostream>
// #include<string>
using namespace std;

int main()
{

    string fname = "Atul", lname = "Tripathi";

    cout<<fname<<" "<<lname;

    // string fullName = fname+" "+lname;
    // cout<<fullName;


    // cout<<fname.length();    // For finding the length of the string

    // cout<<fname.substr(0,2);    // Substring is used for printing the string from one index to other index

    // cout<<fname[0];

    // string myString = "Hello";
    // myString[0] = 'J';
    // cout << myString;

    // fname[0] = 'M';  // use single quote if you use double quote in the fname declaration
    // cout<<fname;


    // string nameOfThePerson;
    // cout<<"Enter your full name here : ";
    // cin>>nameOfThePerson;    // In the only use of cin, user can't enter the name with whitespace

    // getline (cin, nameOfThePerson); // By the use of getline user can use whitespces in input

    // cout<<"Your name is : "<<nameOfThePerson;


    // cout << endl;
    return 0;
}